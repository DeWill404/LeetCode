# Python
```python
class Solution:
	def mySqrt(self, x: int) -> int:
		# Find square root and type cast it to int
		return int(x**0.5)
```
# Java
```java
class Solution {
	public int mySqrt(int x) {
		// Find square root and type cast it to int
		return (int)Math.sqrt(x);
	}
}
```
